<?xml version="1.0" encoding="utf-8"?>
<!--
  ENET/DoIP and OBD-WiFi adapters live on the local network and use plain (non-TLS) links.
  DoIP itself is a raw TCP socket (not affected by this policy), but we still permit cleartext
  to the common private ranges so any HTTP-based adapter tooling works too.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">169.254.0.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
